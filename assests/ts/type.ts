type User = {
  name: string;
  age: number;
  isAdmin: boolean;
};

const user: User = {
  name: "faysal",
  age: 25,
  isAdmin: true,
};

const user2: User = {
  name: "alice",
  age: 30,
  isAdmin: false,
};
console.log(user2);
